<?xml version="1.0"?>
<resource-agent name="pcsmock" version="1.0">
<version>1.1</version>

<longdesc lang="en">
  This is a mock agent for pcs test - minimal agent. It is placed in a real
  provider to cover those test cases where we need a real provider. It is also
  useful for test cases where two providers ship an agent with the same name.
</longdesc>
<shortdesc lang="en">Mock agent for pcs tests - minimal agent</shortdesc>

<actions>
<action name="start"        timeout="20s" />
<action name="stop"         timeout="20s" />
<action name="monitor"      timeout="20s" interval="10s" depth="0"/>
<action name="reload"       timeout="20s" />
<action name="reload-agent" timeout="20s" />
<action name="migrate_to"   timeout="20s" />
<action name="migrate_from" timeout="20s" />
<action name="validate-all" timeout="20s" />
<action name="meta-data"    timeout="5s" />
</actions>
</resource-agent>
